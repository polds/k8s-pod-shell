apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeshell-web.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "kubeshell-web.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
